<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="question">
	<select id="qestionListData" resultType="hashmap">
		select * from Question
	</select>
	<select id="questionDetailData" parameterType="hashmap" resultType="hashmap">
		select * from Question where Qno = ${Qno}
	</select>
	<insert id="questionWriteData" parameterType="hashmap">
		insert into Question 
		(Qtitle,
		 UserId)
		values
		(#{Qtitle},
		 #{UserId})
	</insert>

	<insert id="getWriteData" parameterType="hashmap">
		insert into Question 
		(Qtitle,
		 UserId)
		values
		(#{Qtitle},
		 #{UserId})
	</insert>
</mapper>