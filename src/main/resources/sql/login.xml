<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="login">
   <select id="getData" resultType="hashmap">
      select * from userinfo
   </select>
   <select id="getLoginData" resultType="hashmap" parameterType="hashmap">
      select * from userinfo where UserId = #{id} and UserPw = #{pw}
   </select>
   <select id="checkid" resultType="hashmap">
      select * from userinfo where UserId = #{id}
   </select>
   <insert id="getSignupData" parameterType="hashmap">
      insert into userinfo
      (UserId,
       UserPw,
       UserName,
       UserPost,
       UserGender,
       UserTel,
       UserEmail)
      values
      (#{UserId},
       #{UserPw},
       #{UserName},
       #{UserPost},
       #{UserGender},
       #{UserTel},
       #{UserEmail})
   </insert>
</mapper>